<div id="stars"></div>
<div id="shooting-stars"></div>
<script>
  import { tsParticles } from "@tsparticles/engine";
  import { loadSlim } from "@tsparticles/slim";

  await loadSlim(tsParticles);

  await tsParticles.load({
    id: "stars",
    options: {
      fullScreen: {
        zIndex: -50,
      },
      particles: {
        number: {
          value: 100,
          density: {
            enable: true,
            height: window.innerHeight,
            width: window.innerWidth,
          },
        },
        shape: {
          type: "circle",
        },
        opacity: {
          value: { min: 0.4, max: 1 },
          animation: {
            enable: true,
            speed: 0.5,
            sync: false,
          },
        },
        size: {
          value: { min: 0.8, max: 1.5 },
        },
        move: {
          enable: true,
          speed: { min: 0.01, max: 0.3 },
          direction: "left",
          random: true,
          straight: true,
          outModes: { default: "out" },
        },
      },
    },
  });

  await tsParticles.load({
    id: "shooting-stars",
    options: {
      background: {
        color: "transparent",
      },
      style: {
        opacity: "0.4",
      },
      fullScreen: {
        zIndex: -40,
      },
      particles: {
        number: {
          value: 2,
          density: {
            enable: true,
          },
        },
        color: {
          value: "hsl(254 74% 91%)",
        },
        shape: {
          type: "circle",
        },
        size: {
          value: { min: 0.8, max: 1.5 },
        },
        move: {
          enable: true,
          speed: { min: 3, max: 10 },
          direction: "bottom-right",
          random: true,
          straight: true,
          outModes: { default: "out" },
          trail: {
            enable: true,
            length: 5,
            fill: { color: "hsl(251 5% 8%)" },
          },
        },
      },
    },
  });
</script>

<div
  class="-z-index-8 fixed bottom-0 left-2/3 -translate-x-1/2 -translate-y-8 size-96 rounded-full bg-stone-600 shadow-[0_0_100px_1px_oklch(var(--foreground)/0.1)] overflow-hidden"
>
  <div
    class="absolute size-16 bottom-16 right-17 rounded-full bg-stone-600/50 shadow-[inset_0_0_16px_rgba(0,0,0,0.4)]"
  >
  </div>

  <div
    class="absolute size-8 bottom-48 -right-2 rounded-full bg-stone-600/50 shadow-[inset_0_0_8px_rgba(0,0,0,0.3)] rotate-y-[297deg]"
  >
  </div>

  <div class="absolute size-16 -bottom-8 right-64 rounded-full bg-stone-600/50">
  </div>

  <div
    class="absolute inset-0 rounded-full bg-background shadow-[0_0_100px_60px_oklch(var(--background))] -translate-x-12 -translate-y-12"
  >
  </div>
</div>
